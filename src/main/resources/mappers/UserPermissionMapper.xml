<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mjw.vueback.demos.web.mapper.UserPermissionMapper">

    <select id="findPermissionKeysByUserId" resultType="java.lang.String">
        SELECT permission_key
        FROM user_permissions
        WHERE user_id = #{userId}
        ORDER BY id ASC
    </select>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO user_permissions (user_id, permission_key)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.permissionKey})
        </foreach>
    </insert>

    <delete id="deleteByUserId">
        DELETE FROM user_permissions WHERE user_id = #{userId}
    </delete>

</mapper>
